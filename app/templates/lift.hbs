<TitleBar @title={{model.title}} />

{{#each model.sets as |set|}}
  <WorkoutSet
    @workoutSet={{set}}
    @isCompleted={{get model.lift model.week}} />
{{/each}}

<WorkoutLogger
  @workout={{model.sets.lastObject}}
  @lift={{model.lift}}
  @week={{model.week}} />
